apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    secretRef:
      name: aws-credentials
      namespace: crossplane-system
    source: Secret
