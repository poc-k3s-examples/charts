apiVersion: realm.keycloak.crossplane.io/v1alpha1
kind: Realm
metadata:
  name: homelab-realm
spec:
  providerConfigRef:
    name: keycloak-provider-config
  forProvider:
    realm: homelab
    displayName: Homelab Authentication Realm
    enabled: true
    internalId: homelab
